specVersion: "v1"
serviceId: "donation-center"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "donation-center-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
